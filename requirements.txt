# Core training stack
torch>=2.3.1
transformers>=5.5.0      # Gemma 4 (model_type=gemma4) requires 5.5.0+
peft>=0.18.0             # Gemma4ClippableLinear support
trl>=1.0.0               # SFTTrainer / SFTConfig (stable v1 API)
bitsandbytes>=0.44.0
accelerate>=0.34.0

# Dataset handling
datasets>=3.1.0
huggingface_hub>=0.26.0

# Experiment tracking
wandb>=0.18.0

# Evaluation
sqlparse>=0.5.1
pandas>=2.2.3

# Utilities
tqdm>=4.67.0
python-dotenv>=1.0.1
